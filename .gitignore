# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# `dist/` is deliberately NOT ignored: the offline server has no node and nginx
# serves the committed build straight from the repo. Rebuild and commit it
# before every transfer — see "Деплой" in README.md.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test coverage
coverage

# Vitest/tsc build info
*.tsbuildinfo
